<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listacompra">
    <uses-permission android:name="android.permission.CAMERA"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/iconolista"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconolista"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListaCompra">

        <activity android:name=".NewProduct"></activity>
        <activity android:name=".Carrito" />
        <activity android:name=".Productes" />
        <activity android:name=".ListViewActivity" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ListaCompra.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>